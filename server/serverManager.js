

module.exports = class ServerManager {

	constructor(serverID) {
		

		this.serverID = serverID;
		// In future: expand

	}

	setConfiguration(configObject) {
		
	}


	Permissions = require('./permissionManager.js');

	#serverConfigObj;
	
}